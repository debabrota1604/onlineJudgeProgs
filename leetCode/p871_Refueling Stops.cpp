#include<bits/stdc++.h>
using namespace std;

class Solution{
public:
    vector<int> findBestStop (vector<vector<int>>& stations, int currPos, int maxGas){

    }

    int minRefuelStops(int target, int startFuel, vector<vector<int>>& stations) {

        /*
        Algorithm:
        1. variables: currPos, maxGas, hopCount=0
        2. Initialize currPos = 0, maxDist = startFuel.
        3. Find optimal gas station (pi,fi) between distance (currPos, maxGas). 
            Update  maxDist = maxDist + fi, currDist = pi
        4. If maxDist > target
            return hopCount;





        */

    }
};

int main(){





    return 0;
}